# Conexión a PostgreSQL (Supabase)
DB_HOST=db.xxxxxxxxxxxx.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your-supabase-password
DB_SSL=true

# JWT (RS256) — las claves están en keys/
JWT_EXPIRATION=1h

# Server
PORT=3000
